const replaceDigit = (str: string): string => {
  //   return str.replace(/[0-9]/g, "$");
  return str.replace(/\d/g, "$");
};
console.log("🚀 ~ replaceDigit ~ replaceDigit:", replaceDigit("ADJHD4dfs55j"));
